# 开发进度

## 2026-05-25

- 完成 `P0-WEB-01`：初始化 `frontend/admin_web/` Vite + React + Ant Design 管理后台骨架。
- 根据 `DESIGN.md` 将旧的后台侧边栏占位页替换为品牌化入口主页，包含浮动 pill 导航、hero 区、产品价值、项目亮点、核心闭环和应用场景区域。
- 调整入口页顶部导航：移除 logo mark，将导航恢复为介绍型分区（系统链路、能力地图、当前状态、进入后台），避免入口页和后台功能导航混在一起。
- 将入口页顶部最右侧失效搜索按钮替换为“进入后台”黑色 pill CTA，指向 `/admin` 工作台。
- 按 `DESIGN.md` 规范入口页浮动导航：白色半透明 pill、约 24px 顶部停靠、16px/40px 内边距、居中导航链接组、右侧黑色 pill CTA。
- 优化入口页首屏文案：eyebrow 改为 `INTERACTION ENGINE`，主标题改为“让每一个剧情高光，都成为用户回应的瞬间。”，副文案聚焦关键情绪节点与互动体验。
- 删除入口页首屏右侧状态面板，将 hero 调整为单栏文案区。
- 将入口页“主链路”改为“工作流程”，流程卡片调整为：创建视频、提取高光、审核内容、发布策略、触发互动、回流数据。
- 将首页“能力地图”改为“项目亮点”，并移动到“工作流程”之前；项目亮点卡片文案更新为内容资产、剧情分析、互动编排、审核发布、数据回看。
- 按产品首页结构重写 hero 以下内容：新增产品价值、项目亮点、核心闭环、应用场景四个区块；项目亮点卡片改为纯展示，不再带跳转按钮。
- 按产品官网方向进一步升级首页视觉：首屏加入抽象短剧播放引擎面板，产品价值改为左右分屏 Before/After，对核心闭环做暗色点燃链路，应用场景改为带小型 UI 片段的场景剧场，并移除 footer。
- 优化首屏短剧互动引擎预览：固定面板比例和宽度区间，加入倾斜 2D 透视效果，调整窄屏断点，避免预览在文字下方铺满。
- 引入 `react-router-dom`，将 `/` 与 `/admin/*` 改为正式前端路由；`/admin` 自动重定向到 `/admin/dramas`，并新增短剧管理、剧集配置、AI 高光识别、高光审核发布、仪表盘五个占位页面。
- 将 `/admin` 默认入口调整为 `/admin/dashboard`，进入工作台后默认展示仪表盘页面。
- 简化 `/admin` 顶部栏：移除“后台工作台”副标题和返回入口页按钮，仅保留当前路由页面标题。
- 调整 `/admin` 顶部栏标题样式：标题降为较小字号，并在标题下显示当前模块描述的灰色小字。
- 将后台统计入口路由从 `/admin/analytics` 调整为 `/admin/dashboard`，菜单展示名为“仪表盘”。
- 移除 `/admin/*` 各页面内容区的大圆角占位面板，当前后台页面内容区保持空白，仅保留侧边导航和顶部标题描述。
- 调整 `/admin` 侧边栏：品牌文字不再可点击，副标识改为版本号 `v0.1`，并在底部加入可折叠控制，收起后仅保留图标。
- 将 `/admin` 侧边栏折叠按钮图标改为双箭头：展开时显示向左双箭头，收起后显示向右双箭头。
- 调整 `/admin` 侧边栏收起态：暂时隐藏顶部 `IgniteNow` 标题，避免窄侧栏文字拥挤。
- 调整 `/admin` 侧边栏折叠按钮交互：收起状态下悬停图标使用 Ant Design Tooltip 弹出“展开”，并移除自定义 hover 样式。
- 调整 `/admin` 侧边栏折叠按钮视觉：放大双箭头 SVG 图标，并将按钮形状改为小圆角矩形。
- 调整 `/admin` 侧边栏底部 Tooltip：关闭弹层箭头，并将弹层圆角改为 3px。
- 统一 `/admin` 侧边栏菜单项和折叠按钮视觉规则：所有 SVG 图标统一放大，按钮形状统一为小圆角矩形，并使用同一套 hover 样式。
- 移除 `/admin` 侧边栏底部折叠按钮外层手动 `Tooltip`，折叠按钮仅保留图标和点击行为。
- 将 `/admin` 侧边栏菜单项和折叠按钮圆角统一调整，并显式覆盖选中态圆角。
- 固定 `/admin` 侧边栏菜单和折叠按钮图标容器尺寸，避免侧栏收起动画过程中图标大小变化。
- 对齐 `/admin` 侧边栏底部折叠按钮与菜单项的盒模型，统一宽度、左右边距、高度和内边距。
- 对齐 `/admin` 侧边栏收起态菜单项和底部折叠按钮：统一 40px 宽度、居中 margin、居中图标和 collapsed padding 覆盖。
- 对齐 `/admin` 左侧品牌区与右侧顶部栏高度，统一为 65px，并在两侧底部使用同色灰色分割线。
- 调整 `/admin` 侧边栏收起态品牌区：只隐藏品牌文字，不隐藏品牌区盒子，确保顶部灰色分割线仍显示。
- 新增 `/login` 登录页，视觉风格对齐 `DESIGN.md` 和首页展示页；当前后端 JWT 契约尚未拉取，登录提交暂作为待接入状态。
- 简化 `/login` 登录页背景：移除右下角“高光触发中”预览卡片，仅保留渐变氛围背景和登录表单。
- 进一步简化 `/login` 登录页：移除左侧说明文案区，登录框改为页面中间居中显示。
- 调整 `/login` 顶部返回入口：在“返回入口页”前增加左箭头图标。
- 重写 `frontend/admin_web/AUTHENTICATION.md`，只保留 JWT 认证方案，删除固定 `X-Admin-Token`、Session Cookie 等其它方案说明。
- 补齐前端登录路由闭环：入口页“进入工作台”指向 `/login`，`/admin` 增加前端访问守卫，无本地 access token 时重定向到登录页；登录提交暂写入开发占位 token 并进入 `/admin/dashboard`。
- 调整 `/admin` 顶部品牌区：左侧加入基于登录用户名首字母生成的头像，侧栏收起时仅显示头像；点击头像可打开菜单并执行退出登录，清除本地登录态后回到 `/login`。
- 调整 `/admin` 侧边栏焦点样式：去掉菜单项、折叠按钮、图标和 SVG 的浏览器默认焦点虚线框，避免图标旁出现黄色虚线框。
- 进一步拆分页面职责：`/` 作为介绍型入口页，`/admin` 作为密集型后台工作台骨架；具体后台功能入口已移动到 `/admin` 侧边导航中。
- 验证：`npm install` 成功，`npm exec eslint .` 通过，`npm run build` 通过；本地 dev server 已启动并通过 `http://127.0.0.1:5173/` 返回 200。
- 文档：补充并更新 `frontend/admin_web/README.md`，记录已完成内容、启动方式、访问地址、验证结果、待完善事项和尚未完成范围。

## 遗留问题

- `docs/API_CONTRACT.md`、`docs/HIGHLIGHT_SCHEMA.json` 仍待随后端和跨端契约补齐。
- 管理后台目前完成入口主页、`/admin` 工作台布局和五个 `/admin/*` 空白路由，尚未接入 API client、真实数据和后台业务页面。
